server:
  port: 8088

spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/company
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        


